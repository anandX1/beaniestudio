#!/usr/bin/env node
/**
 * Promo-pack generator — the 10-day sprint's ammunition dump.
 *
 * `npm run promo:pack` regenerates MARKETING-PACK.md at the site root:
 * 30 clip hooks ordered by proven psychology, per-platform captions with
 * hashtag sets, creator DM + Reddit story templates, and the listing-site
 * checklist. Derived from site.config.ts so links can never drift from the
 * live site.
 */
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const here = path.dirname(fileURLToPath(import.meta.url));
const SITE_ROOT = path.resolve(here, '..');

// Pull links straight from the config (regex — config is a tiny TS file).
const cfg = fs.readFileSync(path.join(SITE_ROOT, 'src/site.config.ts'), 'utf8');
const grab = (key) => (cfg.match(new RegExp(`${key}:\\s*'([^']+)'`)) || [])[1] || '';
const SITE_URL = grab('url') || 'https://beaniestudio.site';
const DISCORD = grab('discord') || 'https://discord.gg/z8kPT6cRbG';
const ROBLOX = grab('roblox');
const YT = grab('youtube');
const X = grab('x');
const IG = grab('instagram');
const PLAYTEST = `${SITE_URL}/playtest/`;

const hook = (txt) => `- ${txt}`;

const content = `# MARKETING-PACK.md — 10-Day Sprint Ammunition
> Generated by \`npm run promo:pack\` — regenerate after changing site.config.ts.
> Every link below is current as of generation. Date: ${new Date().toISOString().slice(0, 10)}

## THE MATH (read this before cutting any clip)

**Goal: 10,000 "waiting" humans in 10 days.** Honest funnel: ~3–5% of people who
watch a 30s horror clip click the profile/site; ~20–40% of site visitors join the
Discord or follow the community (yours already does — that's the capture section).
So the sprint is: **maximize watched clips → maximize site visits → capture.**

| Outcome | Pessimistic | Expected | If a clip hits |
|---|---|---|---|
| Discord joins by day 10 | 400–700 | **1,200–2,000** | 5,000+ in a day |
| Roblox community follows | 200–400 | 800–1,500 | 3,000+ |
| Combined "waiting" | ~700 | **~2,500** | 10,000 ✅ |

**Translation: 10k is the *viral-case* outcome. The expected case is ~2.5k — which is
still a top-tier launch for an indie Roblox horror game.** The plan maximizes the
chance of the viral case by shipping volume through the highest-velocity channel.

## THE 30 CLIP HOOKS (film in this order — first 15 are the proven-psychology tier)

### Tier 1 — Fear + curiosity gap (highest velocity)
${[
  'POV: you hear footsteps stop when you stop',
  'The locker rule: never share it with someone who sprints',
  'I hid in the vent. It started breathing. (STATIC playtest)',
  'He sprints once. Watch what the Hunter does.',
  "Don't trust your friend in this game — 60s of why",
  'The wreck has no radar. The Hunter doesn\u2019t need one.',
  'POV: five of you, one exit, sixty seconds',
  'This Roblox horror game listens to your mic-level panic (proximity voice)',
  'Score: everyone survived. Wait for the last 10 seconds.',
  'The fuel is heavy. You cannot run with it. It knows.',
  'Crouch-walking past the Hunter at 0.5x speed (real audio)',
  'The moment the night-vision guy realizes he\u2019s the loudest one left',
  'We tested: can you out-sprint it? (short answer: no)',
  'Extracting with 3 seconds left on the clock (full round)',
  'First-time player experience: 0–100 fear in 40 seconds',
].map(hook).join('\n')}

### Tier 2 — Dev-POV + transparency (highest trust, earns followers)
${[
  'Deleting my own feature: we removed the radar and the game got scarier',
  'How the Hunter AI actually hears you (dev explained, real gameplay)',
  'We rebuilt footsteps 4 times — here\u2019s the final version',
  'Making a horror game where YOU are the monster: Hunter design',
  'Playtesting with strangers: the funniest death so far',
  'Why our horror game runs on a 2016 iPhone (dev optimizes)',
  'The sound design rule: if the player hears it, the Hunter hears it',
  'Adding proximity voice chat was a mistake (funniest bug compilation)',
  'Rating my own game\u2019s fear curve frame by frame',
  'Day X of building STATIC until it launches',
].map(hook).join('\n')}

### Tier 3 — Format plays (comment-bait mechanics)
${[
  'Rate this death 1–10 (comment your score)',
  'Would YOU have opened the locker? (yes/no in comments)',
  'Caption the last 5 seconds. Best one gets pinned.',
  '3 hiding spots ranked worst to best',
  'Ask me anything about the Hunter and I\u2019ll answer in the next clip',
].map(hook).join('\n')}

## PER-PLATFORM CAPTIONS

### TikTok / Reels / Shorts (copy-paste, swap the hook line)
\`\`\`
${'{HOOK}'}

5v1 horror on Roblox. Five salvagers, one Hunter, no radar — only ears.
Free. Crossplay. Q4 2026. Playtest nights now: join the Discord ${DISCORD}
Site + all devlogs: ${SITE_URL}
#roblox #robloxhorror #horrorshorts #indiedev #gamedev #robloxdev #fyp #horrorgaming #5v1 #asymmetricalhorror
\`\`\`

### X / Twitter thread starter
\`\`\`
We're building STATIC — a free 5v1 horror game on Roblox where the monster hunts by SOUND.

No radar. No minimap. Your mic-level panic is a mechanic.

Devlog + playtest nights: ${PLAYTEST}

The core loop in 30 seconds: 👇 [attach clip]
\`\`\`

### Reddit (r/robloxgamedev / r/IndieDev — STORY first, link in comments ONLY)
\`\`\`
Title: We removed the radar from our horror game and playtests got 10x tenser

Body: 3 paragraphs of the real story: what the game is, the design argument,
what changed in playtest behavior, one embarrassing bug. End with a question to
the community. Do NOT put the link in the body — first comment only, or profile.
\`\`\`

### Discord partner-ping template (for servers you ALREADY mod/join)
\`\`\`
Hey [name] — we're opening STATIC (5v1 sound-only horror, Roblox) playtests this week.
Your crew seems perfect for it. Free private slot, all we ask is honest feedback.
Here's the 30s pitch: [clip link] — want in for Friday?
\`\`\`

## CREATOR OUTREACH (10 DMs/day, days 3–9)

**Who:** Roblox horror micro-streamers (500–20k followers; 100k+ ignores you).
**Find them:** TikTok search "roblox horror" + filter recent; Twitch directory
"Roblox"; YouTube search "roblox horror full game" filtered to this month, subs < 20k.
**The DM (personalize ONE line — the bracketed part):**
\`\`\`
Hey [name] — your [specific video] was genuinely tense. We built STATIC: 5v1
sound-only horror on Roblox (no radar, proximity voice — your reactions ARE the
giveaway). We're opening private playtest slots this week and I'd love you in one.
Free obviously — all we ask is honest feedback, post whatever you want or nothing.
Here's 30 seconds of gameplay: [clip] — want a slot Friday?
\`\`\`

## THE 9 LISTING SITES (once, day 1 — ~75 min total)

| Site | What to do |
|---|---|
| Disboard (disboard.org) | Add server w/ tags: roblox, horror, asymmetrical. Add Disboard bot → /bump every 2h someone's awake (set a phone reminder) |
| discord.me | Add server, same tags |
| disforge.com | Add server |
| top.gg/servers | Add server |
| itch.io | Free devlog mirror (post #1 + trailer) |
| IndieDB | Same mirror |
| Reddit | r/robloxgamedev story post (day 2–3, peak hours 14–19 UTC) |
| Roblox DevForum | "Community Resources"/feedback thread with real numbers |
| dump.gg / others | Top off, 10 min |

**The bump chain (unsung hero):** Disboard bump every 2 hours = your listing stays
top-of-tag for the EU + US day. This alone reliably nets 5–20 joins/day for a
polished server with a clear description. Rotate the duty between you + your most
active members (make a #bump-reminders channel).

## LINK BANK (paste anywhere, UTM-attributed)

- Playtest page: ${PLAYTEST}
- Discord: ${DISCORD}
- Roblox community: ${ROBLOX}
- YouTube: ${YT}
- X: ${X}
- Instagram: ${IG}
- UTM format for anything off-site: \`?utm_source=NAME&utm_medium=social&utm_campaign=sprint10k\`

## THE FORBIDDEN LIST (these kill the sprint)

1. ❌ Mass-DMing servers you're not active in (instant ban, zero conversion)
2. ❌ Buying Discord members or using member-farms (dead weight kills Discovery eligibility)
3. ❌ Link-dropping in subreddits without the story post (removal + domain flag)
4. ❌ Spamming the same clip caption everywhere (TikTok suppresses duplicate content)
5. ❌ Botting any platform (Roblox bans communities, Discord bans servers, Google delists)
6. ❌ Promising launch dates you'll miss ("Q4 2026" only — never a day)

## THE DAILY NON-NEGOTIABLES

- [ ] 3 clips posted (TikTok + YT Shorts + Reels — same clip, staggered 2h apart)
- [ ] 10 creator DMs
- [ ] 1 Disboard bump minimum (2h cadence if possible)
- [ ] Reply to every single comment within 2h of posting
- [ ] 15 min in your own Discord VC playing with members (retention > acquisition)
`;

const out = path.join(SITE_ROOT, 'MARKETING-PACK.md');
fs.writeFileSync(out, content);
console.log(`[promo-pack] wrote ${out} (${(content.length / 1024).toFixed(1)} KB)`);
